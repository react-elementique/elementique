import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Navbar from "./Navbar.stories.tsx";

<Meta of={Navbar} title="Navbar" />

# Navbar

Navbar component with different props.

#### Example

<Canvas of={Navbar.Standard} />

## Usage

```ts
import {Navbar} from "elementique";

const Example = () => {
  return (
      <Navbar
        type={"normal"}
        logo={null}
        title={"Navbar"}
        links={[
            {
                label: "Home",
                href: "/home",
            },
            {
                label: "About",
                href: "/about",
            },
        ]}
        className={null}
    />
  );
};

export default Example;
```

#### Arguments

- **type** - 
- **logo** - 
- **title** -
- **links** - 
- **fontsProperty** -
- **className** - 
- **bgColor** - 